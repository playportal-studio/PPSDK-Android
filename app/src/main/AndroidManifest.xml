<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dynepic.ppsdk_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <!-- ToDo: since this isn't an app, we need to remove activities.
        <activity
            android:name="com.dynepic.ppsdk_android.SSOActivity"
            android:label="playPORTAL SSO"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.dynepic.ppsdk_android.LoginActivity"
            android:label="SSO Login"/>
        <activity
            android:name="com.dynepic.ppsdk_android.LogoutActivity"
            android:label="Logout"/>
            -->


    </application>

</manifest>